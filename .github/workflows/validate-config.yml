name: Validate Config

on:
  pull_request:
    branches:
      - zhm

permissions:
  pull-requests: write
  contents: read

jobs:
  validate-config:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Prepare log file
      run: echo "" > log.txt

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.20'

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
        
    - name: Generate JSON schema
      run: |
        echo "🚀 **Running Go code to generate schema.json...**"
        if go run schema/schema.go; then
          echo "  ✅ **JSON schema generated successfully.**"
        else
          echo "  ❌ **Failed to generate JSON schema.**"
          exit 1
        fi

    - name: Install check-jsonschema
      run: |
        echo "📦 **Installing check-jsonschema...**"
        pip install check-jsonschema
        echo -e "\n" >> log.txt

    - name: Validate config.prod.json
      run: |
        echo "🔍 **Validating config.prod.json against schema...**"
        if check-jsonschema --schemafile=schema.json config.prod.json >> log.txt 2>&1; then
          echo "  ✅ **Config Validation successful.**" > log.txt
        else
          echo "  ❌ **Config Validation failed.**" > log.txt
          exit 1
        fi

    - name: Add validation result as comment
      if: always()
      uses: marocchino/sticky-pull-request-comment@v2
      with:
        header: config
        recreate: false
        path: log.txt
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}